# MongoDB Connection
# MongoDB连接
MONGO_DATABASE_URI=mongodb://username:password@host:port/database?retryWrites=true&loadBalanced=false&connectTimeoutMS=10000&authSource=admin&authMechanism=SCRAM-SHA-1

# MySQL Connection (if needed)
# MySQL连接（如果需要）
MYSQL_DATABASE_URL=mysql+aiomysql://username:password@host:port/database?charset=utf8mb4

# UC Backend API
# UC后端API
UC_BACKEND_API_BASE_URL=https://dev-uc.ihealth-eng.com

# Session Token for API authentication (required for image URLs)
# API认证用的会话令牌（获取图片URL必需）
# Get it from: https://portal.ihealthunifiedcare.com/care-portal/home
# -> Inspect -> Application -> Session Storage -> https://portal.ihealth-eng.com -> token
# 获取方式：访问上述网址 -> 开发者工具 -> Application -> Session Storage -> token
SESSION_TOKEN=your_session_token_here

# OpenAI API Key (for AI meal summary analysis)
# OpenAI API密钥（用于AI餐食分析）
# Get it from: https://platform.openai.com/api-keys
# 获取方式：访问 https://platform.openai.com/api-keys 创建API密钥
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
